# Activity logs

One file per day, named `YYYY-MM-DD.md`. The agent writes to these whenever
you mention doing something (now or recently) and logs session topics.

Log files are gitignored — they never leave your machine.

<!-- Exact entry format gets defined in the content-transfer pass. -->
